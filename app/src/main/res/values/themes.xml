<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.OBS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OBS" parent="Base.Theme.OBS" />

    <!-- No Action Bar Theme (for fullscreen activities) -->
    <style name="Theme.OBS.NoActionBar" parent="Base.Theme.OBS" />

    <!-- Splash Screen Theme (No ActionBar) -->
    <style name="Theme.OBS.Splash" parent="Theme.OBS">
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>